<!-- rajoute les update à l'index -->
git add .

<!-- versonner -->
git commit -m "mon commentaire"

<!-- pusher sur un branche, ici master -->
git push origin master

<!-- recuperer modif sur git, ici sur branche master -->
git pull origin master